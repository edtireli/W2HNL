# A parameter file for various dimensions and variables to do with the raw data previously generated
import os

data_folder = 'w2tau_@1'

batch_size = 9999 # Number of events generated per run (mass) in MadGraph

